spring:
  application:
    name: spring-jumarket-stock
  datasource:
    url: jdbc:postgresql://localhost:5435/stock
    username: jumarketST
    password: Stock@1234
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate.format_sql: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    instance:
      preferIpAddress: true
server:
  port: 0

